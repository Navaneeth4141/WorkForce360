'use client';

import React from 'react';
import WorkerLayout from '../../components/WorkerLayout.js';

export default function WorkerRootLayout({ children }) {
  return <WorkerLayout>{children}</WorkerLayout>;
}
